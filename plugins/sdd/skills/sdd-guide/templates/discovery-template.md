# [Project Name] — Discovery

<!-- One-line summary of the project in plain language. -->

## The Idea

<!-- Longer-form description of what is being built. Plain language, no structure imposed. What does it actually do in concrete terms? -->

## Why It Matters

<!-- The motivation behind the project — the itch, the gap, the frustration, the opportunity. What makes this worth doing? -->

## Audience

<!-- Who this is for. If solo-use, say so plainly. If others are intended, name them specifically — who benefits most, who would miss it if it disappeared. -->

## Prior Art and Inspiration

<!-- Existing tools, products, patterns, papers, hobbies, or prior projects that informed the shape of this idea. For each, capture what it gets right and what it gets wrong from the user's perspective. -->

## Constraints

<!-- Limits and non-negotiables already in play — time, tech stack, budget, platform, deployment, regulatory, personal energy. These are surfaced, not yet bounded by /sdd:scope. -->

## Starting Context

<!-- What the user already has on this — existing code, sketches, notes, prior conversations, half-built prototypes. If docs/refs/ was non-empty at command start, note that those files were read and are available to downstream commands. -->

## Open Questions

<!-- Threads the user wanted to flag but did not commit to during the interview. These are signals for /sdd:scope, not commitments. Remove this section if there are no open questions. -->

## Deferred Topics

<!-- Conditional pointer block. Emitted only when docs/backlog.md exists and contains at least one entry (per skills/sdd-guide/references/backlog.md > Parser note). When emitted, the section body is exactly the single line below, verbatim. When the backlog is absent or empty, omit the entire section heading and body — do not leave an empty section behind. Do not inline-list deferred items here; the backlog is the single source of truth. Open Questions above is for unresolved threads still in play for /sdd:scope; this section points at items the user explicitly chose to defer rather than carry forward. -->

Deferred items: see `docs/backlog.md`.
